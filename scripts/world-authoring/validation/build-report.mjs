import { WARN_GLTF_BYTES, WARN_MATERIALS, WARN_TEXTURES, WARN_TRIANGLES } from '../constants.mjs';
export function inspectObject(root) { let triangles=0; const materials=new Set(); const textures=new Set(); root.traverse(o=>{if(!o.isMesh)return; const geometry=o.geometry; if(geometry) triangles+=geometry.index?geometry.index.count/3:(geometry.getAttribute('position')?.count??0)/3; for(const material of Array.isArray(o.material)?o.material:[o.material]){if(!material)continue;materials.add(material.uuid);for(const value of Object.values(material))if(value?.isTexture)textures.add(value.uuid);}}); return {triangles,materials:materials.size,textures:textures.size}; }
export function diagnosticWarnings(name,stats,bytes) { const warnings=[]; if(bytes>WARN_GLTF_BYTES)warnings.push(`${name} is unusually large (${(bytes/1048576).toFixed(1)} MiB).`); if(stats.triangles>WARN_TRIANGLES)warnings.push(`${name} has ${Math.round(stats.triangles)} triangles.`); if(stats.materials>WARN_MATERIALS)warnings.push(`${name} has ${stats.materials} materials.`); if(stats.textures>WARN_TEXTURES)warnings.push(`${name} has ${stats.textures} textures.`); return warnings; }
